server:
  port: 9001
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848 # Nacos地址

# 暴露要监控的东西
management:
  auditevents:
    web:
      exposure:
        include: '*'
